def ai_analysis(workflow):
    return [
        "Workflow assumes all steps succeed without failure handling.",
        "Consider adding retry or rollback mechanisms.",
        "External dependencies should be validated explicitly."
    ]
